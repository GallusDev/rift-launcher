name: FleetDocket CI

on:
  push:
    branches: ["**"]
    paths: ["fleetdocket/**", ".github/workflows/fleetdocket-ci.yml"]
  pull_request:
    paths: ["fleetdocket/**", ".github/workflows/fleetdocket-ci.yml"]

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: fleetdocket
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: fleetdocket/package-lock.json
      - run: npm ci
      - run: npx tsc --noEmit
      - run: npm run lint
      - run: npm run build

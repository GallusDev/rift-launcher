# Filled in at build time from the Gradle project version -- see build.gradle.kts processResources.
# Do not hand-edit: bump `version` in build.gradle.kts instead, so there is only one place to change.
version=${project.version}
